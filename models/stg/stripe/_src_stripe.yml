sources:
  - name: stripe
    description: Clone stripe database
    database: raw
    schema: stripe
    config:
      freshness:
        warn_after:
          count: 6
          period: hour
        error_after:
          count: 12
          period: hour
    tables:
      - name: payments
        description: Raw payments data
        identifier: payments