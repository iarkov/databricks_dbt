macros:
  - name: cents_to_dollars
    description: macro that converts cents to dollars
    arguments:
      - name: column_name
        type: string
        description: name of a column
      - name: decimal
        type: integer
        description: decimal value to round up
