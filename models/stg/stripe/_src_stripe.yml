sources:
  - name: stripe
    database: raw
    schema: stripe
    config:
      freshness:
        warn_after:
          count: 6
          period: hour
        error_after:
          count: 12
          period: hour
    tables:
      - name: payments
        identifier: payments